# https://github.com/marketplace/actions/run-eslint

name: Lint CI
on:
  pull_request:
    branches: [master, main]
jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [14.x]

    steps:
      - run: npm install
      - run: npm run lint:ci
